import { WButton, WButtonGroup, WIcon, WCollapse, WCollapseItem, WAlert, WTooltip, WPopconfirm } from '@whimsical-ui/components'
import type { Plugin } from 'vue'

export default [
    WButton,
    WButtonGroup,
    WIcon,
    WCollapse,
    WCollapseItem,
    WAlert,
    WTooltip,
    WPopconfirm
] as Plugin[]