import { WButton, WButtonGroup, WIcon, WCollapse, WCollapseItem, WAlert, WTooltip, WPopconfirm, WDropdown, WDropdownItem, WConfigProvider } from '@whimsical-ui/components'
import type { Plugin } from 'vue'

export default [
    WButton,
    WButtonGroup,
    WIcon,
    WCollapse,
    WCollapseItem,
    WAlert,
    WTooltip,
    WPopconfirm,
    WDropdown,
    WDropdownItem,
    WConfigProvider
] as Plugin[]