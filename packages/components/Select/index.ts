import Select from './Select.vue'
import Option from './Option.vue'
import { withInstall } from '@whimsical-ui/utils'

export const WSelect = withInstall(Select)
export const WOption = withInstall(Option)

export * from './types'